sudo: required
services:
    - docker

before_install:
    - docker build -t laudes/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true laudes/docker-react npm run test -- --coverage